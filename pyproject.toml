[project]
name = "testpilot"
version = "0.1.0"
description = "AI assisted API test automation platform MVP"
requires-python = ">=3.8"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
